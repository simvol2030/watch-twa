# План расширения до интернет-магазина

> **Дата создания:** 2025-12-06
> **Статус:** В разработке
> **Платформа:** Telegram Web App (только мобильная версия)

---

## Общая концепция

Расширение существующей системы лояльности до универсального интернет-магазина, который можно адаптировать под разные типы бизнеса (рестораны, зоотовары, одежда и т.д.).

### Ключевые принципы:
- **Mobile-first** — только Telegram Web App, никакого десктопа
- **Универсальность** — шаблон для разных проектов с разными Telegram ботами
- **Простота** — базовый функционал без сложных вариаций товаров (пока)
- **Гибкость** — настраиваемые режимы доставки/самовывоза

---

## Принятые решения

| Вопрос | Решение |
|--------|---------|
| **Доставка** | Настраиваемо: только доставка / только самовывоз / оба варианта |
| **Оплата** | Только "при получении" (интеграции позже) |
| **Авторизация** | Гостевой checkout + привязка к loyalty-аккаунту |
| **Остатки** | Не ведём (реализуем позже) |
| **Статусы заказов** | Стандартный: Новый → Подтверждён → В обработке → Отправлен → Доставлен |
| **Telegram бот** | Настраивается в админке (разные боты для разных проектов) |
| **Уведомления клиенту** | Опционально (вкл/выкл в настройках) |
| **Вариации товаров** | Заложить в схему, но не реализовывать сейчас |

---

## Структура базы данных

### Новые таблицы

#### 1. `categories` — Категории товаров
```sql
CREATE TABLE categories (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  slug TEXT UNIQUE NOT NULL,
  description TEXT,
  image TEXT,
  parent_id INTEGER REFERENCES categories(id),  -- для подкатегорий
  position INTEGER DEFAULT 0,                    -- сортировка
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2. `cart_items` — Корзина
```sql
CREATE TABLE cart_items (
  id INTEGER PRIMARY KEY,
  session_id TEXT,                              -- для гостей (cookie)
  user_id INTEGER REFERENCES loyalty_users(id), -- для авторизованных
  product_id INTEGER NOT NULL REFERENCES products(id),
  quantity INTEGER DEFAULT 1,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 3. `orders` — Заказы
```sql
CREATE TABLE orders (
  id INTEGER PRIMARY KEY,
  order_number TEXT UNIQUE NOT NULL,            -- #00001
  user_id INTEGER REFERENCES loyalty_users(id), -- nullable для гостей

  -- Статус
  status TEXT DEFAULT 'new',                    -- new/confirmed/processing/shipped/delivered/cancelled

  -- Контакты клиента
  customer_name TEXT NOT NULL,
  customer_phone TEXT NOT NULL,
  customer_email TEXT,

  -- Доставка
  delivery_type TEXT NOT NULL,                  -- pickup/delivery
  delivery_address TEXT,                        -- для доставки
  delivery_entrance TEXT,                       -- подъезд
  delivery_floor TEXT,                          -- этаж
  delivery_apartment TEXT,                      -- квартира
  delivery_intercom TEXT,                       -- домофон
  store_id INTEGER REFERENCES stores(id),       -- для самовывоза

  -- Суммы
  subtotal INTEGER NOT NULL,                    -- сумма товаров (копейки)
  delivery_cost INTEGER DEFAULT 0,              -- стоимость доставки
  discount_amount INTEGER DEFAULT 0,            -- скидка (баллы лояльности)
  total INTEGER NOT NULL,                       -- итого

  -- Дополнительно
  notes TEXT,                                   -- комментарий к заказу
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 4. `order_items` — Позиции заказа
```sql
CREATE TABLE order_items (
  id INTEGER PRIMARY KEY,
  order_id INTEGER NOT NULL REFERENCES orders(id),
  product_id INTEGER REFERENCES products(id),

  -- Снапшот на момент заказа
  product_name TEXT NOT NULL,
  product_price INTEGER NOT NULL,
  quantity INTEGER NOT NULL,
  total INTEGER NOT NULL,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 5. `shop_settings` — Настройки магазина
```sql
CREATE TABLE shop_settings (
  id INTEGER PRIMARY KEY DEFAULT 1,

  -- Основные
  shop_name TEXT DEFAULT 'Магазин',
  shop_type TEXT DEFAULT 'general',             -- restaurant/pet_shop/clothing/general

  -- Доставка
  delivery_enabled BOOLEAN DEFAULT true,        -- включена ли доставка
  pickup_enabled BOOLEAN DEFAULT true,          -- включён ли самовывоз
  delivery_cost INTEGER DEFAULT 0,              -- стоимость доставки (копейки)
  free_delivery_from INTEGER,                   -- бесплатная доставка от суммы
  min_order_amount INTEGER DEFAULT 0,           -- минимальная сумма заказа

  -- Telegram бот
  telegram_bot_token TEXT,                      -- токен бота
  telegram_bot_username TEXT,                   -- @username бота

  -- Уведомления
  telegram_notifications_enabled BOOLEAN DEFAULT false,
  telegram_group_id TEXT,                       -- ID группы для уведомлений
  email_notifications_enabled BOOLEAN DEFAULT false,
  email_recipients TEXT,                        -- JSON array emails
  customer_telegram_notifications BOOLEAN DEFAULT false, -- уведомлять клиента о статусе

  -- SMTP для email
  smtp_host TEXT,
  smtp_port INTEGER,
  smtp_user TEXT,
  smtp_password TEXT,
  smtp_from TEXT,

  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Изменения в существующих таблицах

#### `products` — добавить поля
```sql
ALTER TABLE products ADD COLUMN category_id INTEGER REFERENCES categories(id);
ALTER TABLE products ADD COLUMN sku TEXT;                    -- артикул
ALTER TABLE products ADD COLUMN position INTEGER DEFAULT 0;  -- позиция в категории
ALTER TABLE products ADD COLUMN has_variants BOOLEAN DEFAULT false; -- для будущего
```

---

## Фазы реализации

### Фаза 1: База данных и категории
**Приоритет:** Высокий

- [ ] 1.1. Создать миграцию для новых таблиц
- [ ] 1.2. Обновить Drizzle схему
- [ ] 1.3. Добавить category_id в products
- [ ] 1.4. API для категорий (CRUD)
- [ ] 1.5. Админка: управление категориями
- [ ] 1.6. Админка: drag & drop сортировка категорий
- [ ] 1.7. Админка: привязка товаров к категориям
- [ ] 1.8. Публичный каталог с фильтром по категориям

### Фаза 2: Корзина
**Приоритет:** Высокий

- [ ] 2.1. API корзины (add/update/remove/clear/get)
- [ ] 2.2. Svelte store для состояния корзины
- [ ] 2.3. Компонент CartIcon в header (badge с количеством)
- [ ] 2.4. Компонент CartDropdown (мини-корзина)
- [ ] 2.5. Страница /cart (полная корзина)
- [ ] 2.6. Кнопка "В корзину" на карточках товаров
- [ ] 2.7. Сохранение корзины гостя в cookie/localStorage

### Фаза 3: Оформление заказа
**Приоритет:** Высокий

- [ ] 3.1. Страница /checkout
- [ ] 3.2. Выбор типа доставки (доставка/самовывоз)
- [ ] 3.3. Форма для доставки (адрес, подъезд, этаж и т.д.)
- [ ] 3.4. Форма для самовывоза (выбор магазина)
- [ ] 3.5. Контактные данные (имя, телефон, email)
- [ ] 3.6. Комментарий к заказу
- [ ] 3.7. API создания заказа
- [ ] 3.8. Генерация номера заказа
- [ ] 3.9. Страница "Заказ оформлен" с номером

### Фаза 4: Админка заказов
**Приоритет:** Высокий

- [ ] 4.1. Страница /admin/orders — список заказов
- [ ] 4.2. Фильтры (по статусу, дате)
- [ ] 4.3. Детальная страница заказа
- [ ] 4.4. Изменение статуса заказа
- [ ] 4.5. Просмотр истории изменений статуса

### Фаза 5: Настройки магазина
**Приоритет:** Средний

- [ ] 5.1. Страница настроек магазина в админке
- [ ] 5.2. Настройка режимов доставки (вкл/выкл)
- [ ] 5.3. Настройка стоимости доставки
- [ ] 5.4. Настройка бесплатной доставки от суммы
- [ ] 5.5. Настройка минимальной суммы заказа
- [ ] 5.6. Настройка Telegram бота (токен, username)

### Фаза 6: Уведомления
**Приоритет:** Средний

- [ ] 6.1. Сервис отправки в Telegram группу
- [ ] 6.2. Форматирование сообщения о заказе
- [ ] 6.3. Настройка Telegram группы в админке
- [ ] 6.4. Сервис отправки email (nodemailer)
- [ ] 6.5. HTML шаблон письма о заказе
- [ ] 6.6. Настройка SMTP в админке
- [ ] 6.7. Уведомление клиента о статусе в Telegram (опционально)

### Фаза 7: Импорт товаров
**Приоритет:** Низкий

- [ ] 7.1. Парсер CSV файлов
- [ ] 7.2. Парсер JSON файлов
- [ ] 7.3. UI импорта в админке
- [ ] 7.4. Валидация данных при импорте
- [ ] 7.5. Режим обновления существующих (по SKU)
- [ ] 7.6. Автоматическое создание категорий
- [ ] 7.7. Скачивание шаблонов (CSV/JSON)
- [ ] 7.8. Экспорт товаров

### Фаза 8: История покупок клиента
**Приоритет:** Низкий

- [ ] 8.1. Страница /orders — список заказов клиента
- [ ] 8.2. Детальная страница заказа для клиента
- [ ] 8.3. Повторный заказ (добавить товары в корзину)

---

## Структура компонентов (Frontend)

```
src/routes/
├── (app)/
│   ├── +layout.svelte          # Основной layout с header
│   ├── products/
│   │   ├── +page.svelte        # Каталог с категориями
│   │   └── [id]/+page.svelte   # Карточка товара
│   ├── cart/
│   │   └── +page.svelte        # Полная страница корзины
│   ├── checkout/
│   │   └── +page.svelte        # Оформление заказа
│   ├── orders/
│   │   ├── +page.svelte        # Список заказов клиента
│   │   ├── [id]/+page.svelte   # Детали заказа
│   │   └── success/+page.svelte # Заказ оформлен
│   └── ...
│
├── (admin)/
│   ├── orders/
│   │   ├── +page.svelte        # Список заказов
│   │   └── [id]/+page.svelte   # Детали заказа
│   ├── categories/
│   │   └── +page.svelte        # Управление категориями
│   ├── shop-settings/
│   │   └── +page.svelte        # Настройки магазина
│   ├── import/
│   │   └── +page.svelte        # Импорт товаров
│   └── ...

src/lib/
├── components/
│   ├── cart/
│   │   ├── CartIcon.svelte     # Иконка в header
│   │   ├── CartDropdown.svelte # Выпадающая мини-корзина
│   │   ├── CartItem.svelte     # Товар в корзине
│   │   └── CartSummary.svelte  # Итоги корзины
│   ├── checkout/
│   │   ├── DeliveryForm.svelte # Форма доставки
│   │   ├── PickupForm.svelte   # Форма самовывоза
│   │   └── CustomerForm.svelte # Контактные данные
│   └── catalog/
│       ├── CategoryFilter.svelte
│       ├── ProductCard.svelte
│       └── AddToCartButton.svelte
│
├── stores/
│   └── cart.ts                 # Svelte store для корзины
│
└── services/
    ├── cart.ts                 # API корзины
    ├── orders.ts               # API заказов
    └── notifications.ts        # Сервис уведомлений
```

---

## API Endpoints

### Корзина
```
GET    /api/cart              — получить корзину
POST   /api/cart/add          — добавить товар { productId, quantity }
PATCH  /api/cart/:itemId      — изменить количество { quantity }
DELETE /api/cart/:itemId      — удалить товар
DELETE /api/cart              — очистить корзину
```

### Заказы (публичные)
```
POST   /api/orders            — создать заказ
GET    /api/orders            — список заказов клиента
GET    /api/orders/:id        — детали заказа
```

### Заказы (админ)
```
GET    /api/admin/orders              — список всех заказов
GET    /api/admin/orders/:id          — детали заказа
PATCH  /api/admin/orders/:id/status   — изменить статус
```

### Категории
```
GET    /api/categories                — список категорий
GET    /api/admin/categories          — список для админки
POST   /api/admin/categories          — создать
PATCH  /api/admin/categories/:id      — обновить
DELETE /api/admin/categories/:id      — удалить
PATCH  /api/admin/categories/reorder  — изменить порядок
```

### Настройки магазина
```
GET    /api/admin/shop-settings       — получить настройки
PATCH  /api/admin/shop-settings       — обновить настройки
```

### Импорт
```
POST   /api/admin/import/products     — импорт товаров
GET    /api/admin/export/products     — экспорт товаров
GET    /api/admin/import/template/:format — скачать шаблон
```

---

## Статусы заказов

| Статус | Название (RU) | Описание |
|--------|---------------|----------|
| `new` | Новый | Только что создан |
| `confirmed` | Подтверждён | Менеджер подтвердил |
| `processing` | В обработке | Собирается/готовится |
| `shipped` | Отправлен | Передан в доставку |
| `delivered` | Доставлен | Получен клиентом |
| `cancelled` | Отменён | Заказ отменён |

---

## Формат уведомлений

### Telegram группа (новый заказ)
```
📦 НОВЫЙ ЗАКАЗ #00142

👤 Иван Петров
📞 +7 (999) 123-45-67
📧 ivan@example.com

🛒 Товары:
• Корм Royal Canin 15кг × 1 — 4 500 ₽
• Миска для собак × 2 — 1 780 ₽

💰 Итого: 6 280 ₽
🚚 Самовывоз: магазин "Алмаз"

📝 Позвоните за час до
```

### Telegram клиенту (изменение статуса)
```
✅ Ваш заказ #00142 подтверждён!

Ожидайте звонка менеджера для уточнения деталей.
```

---

## Технические заметки

### Хранение корзины гостя
- `session_id` генерируется при первом добавлении в корзину
- Хранится в httpOnly cookie (7 дней)
- При авторизации — мержим корзину гостя с корзиной пользователя

### Генерация номера заказа
```typescript
// Формат: #YYMMDD-XXX (год-месяц-день + порядковый номер за день)
// Пример: #241206-001, #241206-002
function generateOrderNumber(): string {
  const date = new Date();
  const prefix = date.toISOString().slice(2, 10).replace(/-/g, '');
  const count = await getOrderCountForToday();
  return `#${prefix}-${String(count + 1).padStart(3, '0')}`;
}
```

### Валидация телефона
```typescript
// Российский формат: +7 (XXX) XXX-XX-XX
const phoneRegex = /^\+7\s?\(?\d{3}\)?\s?\d{3}[-\s]?\d{2}[-\s]?\d{2}$/;
```

---

## Зависимости для добавления

```json
{
  "dependencies": {
    "nodemailer": "^6.9.x",      // Email отправка
    "csv-parse": "^5.5.x",       // Парсинг CSV
    "uuid": "^9.0.x"             // Генерация session_id
  }
}
```

---

## Будущие улучшения (не в текущем скоупе)

- [ ] Онлайн-оплата (ЮKassa, Тинькофф, СБП)
- [ ] Учёт остатков товаров
- [ ] Вариации товаров (размеры, цвета)
- [ ] Модификаторы для ресторанов (добавки к блюдам)
- [ ] Синхронизация с 1С (остатки, цены)
- [ ] Push-уведомления
- [ ] Промокоды и купоны
- [ ] Программа лояльности при покупке (интеграция с текущей)
- [ ] Отзывы на товары
- [ ] Избранное

---

## Changelog

| Дата | Изменения |
|------|-----------|
| 2025-12-06 | Первоначальный план создан |
